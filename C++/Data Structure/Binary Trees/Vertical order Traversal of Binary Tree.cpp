#include iostream
#include vector
using namespace std;

int main() {
	code
	
	int t;
	cint;
	int n[t];
	vectorint a[t];
	for(int j=0;jt;j++){
	    cinn[j];
	    for(int k=0;kn[j];k++){
	        int x;
	        cinx;
	        a[j].push_back(x);
	    }
	    
	}
	
	int i=0;
	while(it){
	    vectorint v;
	    for(int j=0;jn[i]-1;j++){
	        int sum=a[i].at(j);
	        for(int k=0;kn[i];k++){
	            sum=sum+a[i].at(k);
	            v.push_back(sum);
	        }
	    }
	    int max=v.at(0);
	    int p=0;
	    for(int j=0;jv.size();j++){
	        if(a[i].at(j)max){
	            max=a[i].at(j);
	        }
	    }
	    coutmaxendl;
	}
}